import { useEffect,useState } from 'react';import { supabase } from '../lib/supabaseClient';export default function Categoria(){const [cats,setCats]=useState([]);const [name,setName]=useState('');useEffect(()=>load(),[]);async function load(){const { data } = await supabase.from('categories').select('*').order('id'); setCats(data||[])}async function add(){if(!name) return; await supabase.from('categories').insert([{ name }]); setName(''); load()}return (<div className='container'><h2>Categorías</h2><div className='card'><div style={{display:'flex',gap:8}}><input value={name} onChange={e=>setName(e.target.value)}/><button onClick={add}>Agregar</button></div><ul>{cats.map(c=>(<li key={c.id}>{c.name}</li>))}</ul></div></div>)}
