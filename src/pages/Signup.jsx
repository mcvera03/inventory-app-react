import React,{useState} from 'react'; import { supabase } from '../services/supabaseClient'; import { useNavigate } from 'react-router-dom'; export default function Signup(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [name,setName]=useState(''); const nav=useNavigate(); const handleSignup=async(e)=>{ e.preventDefault(); const { data, error } = await supabase.auth.signUp({ email, password }); if(error) return alert(error.message); if(data?.user) await supabase.from('profiles').insert({ id:data.user.id, full_name: name }); nav('/login') } return (<div style={{maxWidth:480, margin:'30px auto'}}><h2>Crear cuenta</h2><form onSubmit={handleSignup}><input placeholder='Correo' value={email} onChange={e=>setEmail(e.target.value)} style={{width:'100%',padding:8}}/><input placeholder='Nombre' value={name} onChange={e=>setName(e.target.value)} style={{width:'100%',padding:8,marginTop:8}}/><input placeholder='Contraseña' type='password' value={password} onChange={e=>setPassword(e.target.value)} style={{width:'100%',padding:8,marginTop:8}}/><div style={{textAlign:'center',marginTop:12}}><button>Crear cuenta</button></div></form></div>)}