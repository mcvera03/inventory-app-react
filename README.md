# Inventory App (React + Supabase)

This project is prepared for deployment to Netlify and integration with Supabase.  
Replace `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` in Netlify environment variables or a local `.env` when developing locally.
